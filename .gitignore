node_modules/
dist/
.DS_Store
.env
.vite/
.superpowers/
*.psd
*.mov
*.mp4
previous/
WordPress/
**/original-upload/
*.pdf
*.psd
*.mov
*.mp4
.superpowers/
WordPress/
previous/
docs/superpowers/
assets/playground/**/original-upload/

# Local scratch / working files — not part of the deployed site
backups/
参考/
Page Pics/
.Rhistory

# Local experiments / per-machine config
linedemo/
pencil/
.claude/settings.local.json

# Deployed video assets. The blanket *.mov/*.mp4 rules above keep raw local
# footage out of the repo, but anything index.html actually references has to
# ship or the deployed site 404s it. Negations must stay below those rules.
!assets/home/home-video.mp4
